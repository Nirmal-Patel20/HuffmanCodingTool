#include "huffman.hpp"
#include <cstring>

namespace fs = std::filesystem;
namespace UIHelper {

    const char* get_exe_name(const char *path);

    std::string checkValidOptions(int argc, char* argv[]);
}
